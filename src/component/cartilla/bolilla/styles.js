import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderColor: 'gray',
    borderWidth: 0.5,
    borderRadius: 7,
    marginHorizontal: 4,
    marginVertical: 2,
    height: 30,
    justifyContent: 'center',
  },
  label: {
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
