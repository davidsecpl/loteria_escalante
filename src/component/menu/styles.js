import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        marginVertical: 5,
        flexDirection: "row",
        justifyContent: "space-around",
        alignItems: "center"
    },
    label:{
        fontSize: 16,
        fontStyle: "italic",
        color: 'gray'
    }
});